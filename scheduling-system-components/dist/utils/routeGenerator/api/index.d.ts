import { Entity } from '../../../interfaces/types';
export declare const generateApiRoutes: (config: Entity) => {
    list: string;
    create: string;
    edit: string;
    view: string;
    delete: string;
};
