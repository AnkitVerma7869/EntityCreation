"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateViewPage = generateViewPage;
function generateViewPage(config) {
    return "\n    'use client';\n    import { useEffect } from 'react';\n    import { useRouter } from 'next/navigation';\n    import { use".concat(config.entityName, "Store } from '@/store/").concat(config.entityName.toLowerCase(), "Store';\n\n    export default function ").concat(config.entityName, "ViewPage({ params }: { params: { id: string } }) {\n      const router = useRouter();\n      const { currentRecord, loading, error, fetchRecord } = use").concat(config.entityName, "Store();\n\n      useEffect(() => {\n        fetchRecord(params.id);\n      }, [params.id]);\n\n      if (loading) return <div>Loading...</div>;\n      if (!currentRecord) return <div>Record not found</div>;\n\n      return (\n        <div className=\"p-6 max-w-2xl mx-auto\">\n          <div className=\"flex justify-between items-center mb-6\">\n            <h1 className=\"text-2xl font-bold\">").concat(config.entityName, " Details</h1>\n            <button\n              onClick={() => router.back()}\n              className=\"px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600\"\n            >\n              Back\n            </button>\n          </div>\n\n          <div className=\"bg-white shadow rounded-lg p-6\">\n            <dl className=\"space-y-4\">\n              ").concat(config.attributes.map(function (attr) { return "\n                <div>\n                  <dt className=\"text-sm font-medium text-gray-500\">".concat(attr.name, "</dt>\n                  <dd className=\"mt-1 text-sm text-gray-900\">\n                    {currentRecord.").concat(attr.name.replace(/\s+/g, '_'), "}\n                  </dd>\n                </div>\n              "); }).join(''), "\n            </dl>\n          </div>\n        </div>\n      );\n    }\n  ");
}
